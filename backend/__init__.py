# This empty file tells Python that the 'backend' folder contains code that can be imported.
# Without this file, Python treats the folder as just a regular directory and can't use the code inside it. ss
# This file acts like a label that says "this folder has reusable code".
